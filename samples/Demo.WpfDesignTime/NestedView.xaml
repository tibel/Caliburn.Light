<UserControl x:Class="Demo.WpfDesignTime.NestedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="https://github.com/tibel/Caliburn.Light/"
             xmlns:local="clr-namespace:Demo.WpfDesignTime"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:NestedViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label FontSize="16" Background="LightGreen" Content="{Binding Name, Mode=OneWay}" />
    </Grid>
</UserControl>
