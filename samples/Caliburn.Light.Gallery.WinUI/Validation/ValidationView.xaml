<UserControl x:Class="Caliburn.Light.Gallery.WinUI.Validation.ValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Name:" Grid.Row="0" VerticalAlignment="Center" Margin="3" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="0" Grid.Column="1" Margin="0,2,40,2" />

        <TextBlock Text="Address:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="3" />
        <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="1" Grid.Column="1" Margin="0,2,40,2" />

        <TextBlock Text="Contact:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="3" />
        <TextBox Text="{Binding Contact, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="2" Grid.Column="1" Margin="0,2,40,2" />

        <TextBlock Text="Website:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="3" />
        <TextBox Text="{Binding Website, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="3" Grid.Column="1" Margin="0,2,40,2" />

        <Button Command="{Binding SaveCommand}" Content="Save" Padding="5" Grid.Row="4" Grid.Column="1" Margin="0,10,40,0" HorizontalAlignment="Right" VerticalAlignment="Top" />
    </Grid>
</UserControl>
