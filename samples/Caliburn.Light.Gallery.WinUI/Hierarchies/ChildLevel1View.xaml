<UserControl x:Class="Caliburn.Light.Gallery.WinUI.Hierarchies.ChildLevel1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="using:Caliburn.Light.WinUI"
             xmlns:local="using:Caliburn.Light.Gallery.WinUI.Hierarchies"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <ContentControl HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    DataContext="{Binding ActiveItem, Mode=OneWay}"
                    cal:View.Create="True" />
</UserControl>
