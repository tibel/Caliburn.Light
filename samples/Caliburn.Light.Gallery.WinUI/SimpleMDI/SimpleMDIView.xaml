<UserControl x:Class="Caliburn.Light.Gallery.WinUI.SimpleMDI.SimpleMDIView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="using:Caliburn.Light.WinUI"
             xmlns:local="using:Caliburn.Light.Gallery.WinUI.SimpleMDI"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Content="Open Tab" Command="{Binding OpenTabCommand, Mode=OneTime}" Grid.Row="0" />

        <Pivot Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding ActiveItem, Mode=TwoWay}">
            <Pivot.HeaderTemplate>
                <DataTemplate x:DataType="local:TabViewModel">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}" />
                        <Button Content="X"
                                Command="{x:Bind CloseCommand, Mode=OneTime}"
                                Padding="4,2"
                                Margin="8,0,0,0" />
                    </StackPanel>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.ItemTemplate>
                <DataTemplate x:DataType="local:TabViewModel">
                    <local:TabView cal:View.Bind="True" />
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>
    </Grid>
</UserControl>
