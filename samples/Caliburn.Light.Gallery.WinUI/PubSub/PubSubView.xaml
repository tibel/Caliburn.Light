<UserControl x:Class="Caliburn.Light.Gallery.WinUI.PubSub.PubSubView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="using:Caliburn.Light.WinUI"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="using:Caliburn.Light.Gallery.WinUI.PubSub"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:PublisherView Grid.Column="0"
                             DataContext="{Binding Publisher, Mode=OneWay}"
                             cal:View.Bind="True" />

        <local:SubscriberView Grid.Column="1"
                              DataContext="{Binding Subscriber, Mode=OneWay}"
                              cal:View.Bind="True" />
    </Grid>
</UserControl>
